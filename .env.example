# Thalex — Environment variables template.
# Copy to `.env` (which is gitignored) and fill in real values.
# Only VITE_* variables are exposed to the browser bundle.

# Public WhatsApp business number (without "+"), used for activation links.
VITE_WHATSAPP_NUMBER=593993703912

# Canonical site URL used for SEO meta tags and og:url.
VITE_SITE_URL=https://www.thalexec.com
